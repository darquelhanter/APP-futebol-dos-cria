import { Player, Pelada, NotificationLog } from '../types';

export const INITIAL_PLAYERS: Player[] = [];
export const INITIAL_PELADAS: Pelada[] = [];
export const INITIAL_NOTIFICATIONS: NotificationLog[] = [];

export const initialPelada: Pelada | null = null;
export const initialPeladas: Pelada[] = [];
export const initialPlayers: Player[] = [];
export const initialNotifications: NotificationLog[] = [];
