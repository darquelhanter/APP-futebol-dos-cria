{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Dados do perfil de usuário autenticado via Google",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "ID único do usuário no Firebase Auth" },
        "displayName": { "type": "string", "description": "Nome completo ou apelido do usuário" },
        "email": { "type": "string", "description": "E-mail Google do usuário" },
        "photoURL": { "type": "string", "description": "Foto de perfil do Google" },
        "lastLogin": { "type": "string", "description": "Data/hora do último acesso" }
      },
      "required": ["uid", "email"]
    },
    "Pelada": {
      "title": "Pelada",
      "description": "Partida/pelada agendada com participantes, times, financeiro e estatísticas",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Identificador da pelada" },
        "title": { "type": "string", "description": "Título da pelada" },
        "date": { "type": "string", "description": "Data no formato YYYY-MM-DD" },
        "time": { "type": "string", "description": "Horário de início" },
        "location": { "type": "string", "description": "Local/Quadra" },
        "maxPlayers": { "type": "number", "description": "Capacidade máxima de jogadores" },
        "status": { "type": "string", "enum": ["agendada", "ao_vivo", "finalizada", "cancelada"] }
      },
      "required": ["id", "title", "date", "status"]
    },
    "PlayersCollection": {
      "title": "PlayersCollection",
      "description": "Lista de jogadores cadastrados no elenco da pelada",
      "type": "object",
      "properties": {
        "players": { "type": "string", "description": "Array serializado com os jogadores e atributos FUT" }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Coleção de perfis de usuários do aplicativo"
    },
    "/peladas/{peladaId}": {
      "schema": "Pelada",
      "description": "Coleção de peladas com escalações e financeiro"
    },
    "/app_data/{docId}": {
      "schema": "PlayersCollection",
      "description": "Documentos globais do grupo (elenco, configurações)"
    }
  }
}
